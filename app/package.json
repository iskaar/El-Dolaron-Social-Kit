{
  "name": "el-dolaron-app",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"src/**/*.test.ts\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "db:local": "wrangler d1 execute el-dolaron --local --file=schema.sql",
    "db:remote": "wrangler d1 execute el-dolaron --remote --file=schema.sql"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0"
  }
}
